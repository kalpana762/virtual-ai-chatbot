```javascript\n// Demonstrating how to parse query parameters using the URL module in Node.js\n\nconst http = require('http');\nconst url = require('url');\n\nconst server = http.createServer((req, res) => {\n  // Parse the incoming request URL\n  // 'true' ensures parsedUrl.query is an object we can easily read\n  const parsedUrl = url.parse(req.url, true);\n\n  if (parsedUrl.pathname === '/test' && req.method === 'GET') {\n    // Example: /test?name=John\n    const name = parsedUrl.query.name;\n    const message = name ? `Hello, ${name}!` : 'Hello, anonymous!';\n\n    res.statusCode = 200;\n    res.setHeader('Content-Type', 'text/plain');\n    res.end(`Query parameter 'name': ${message}`);\n    return;\n  }\n\n  res.statusCode = 404;\n  res.setHeader('Content-Type', 'text/plain');\n  res.end('Not Found');\n});\n\nserver.listen(3001, () => {\n  console.log('Server running at http://localhost:3001/');\n});\n```